interface Quote {
  policyName: string,
  breed: string,
  basePrice: number,
  finalPrice: number,
  breedModifier: number,
  excessCorrection: number,
  petModifier: number,
  ageCorrection: number

}

export default Quote;
